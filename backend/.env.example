# Banco de dados PostgreSQL
DB_USER=postgres
DB_HOST=localhost
DB_NAME=dbSistemaFilial
DB_PASSWORD=sua_senha
DB_PORT=5432

# Servidor
PORT=3000
NODE_ENV=development

# CORS — em produção, coloque a URL do seu frontend (ex: https://seusite.com)
CORS_ORIGIN=*
